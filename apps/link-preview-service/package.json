{
  "name": "doughatcher-link-preview",
  "version": "1.0.0",
  "description": "Cloudflare Worker — link preview metadata API for doughatcher.com",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 8790",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
