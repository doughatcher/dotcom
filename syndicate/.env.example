# Syndication credentials. Copy to syndicate/.env (gitignored) and fill in.
# In CI these come from GitHub Actions secrets — set the same names in
# Settings → Secrets and variables → Actions.

# dev.to — generate at https://dev.to/settings/extensions (doughatcher account)
DEVTO_API_KEY=
